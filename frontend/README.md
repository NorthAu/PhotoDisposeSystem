# 前端使用说明

该前端基于 Vue 3 + Vite，用于演示后端 REST API 的核心功能。

## 安装与运行
```bash
cd frontend
npm install
npm run dev
```

默认访问地址：`http://localhost:5173`

## 说明
- 已配置 Vite 代理到后端 `http://localhost:8080`。
- 请确保后端先启动，否则请求会失败。
